# Go CI with test and build
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/wyattau/runner-images/go:1
    steps:
      - uses: actions/checkout@v4
      - run: go version
      - run: go test ./...
      - run: go build -o bin/app .
